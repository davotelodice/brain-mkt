# ============================================
# Marketing Second Brain - Environment Variables
# ============================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values below
# 3. NEVER commit .env to git!
#
# ============================================

# ----- Database (Supabase) -----
# Get these from: Supabase Dashboard > Settings > API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ----- Authentication -----
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=generate-a-secure-random-string-here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ----- LLM Provider -----
# Options: openai, openrouter
LLM_PROVIDER=openai

# OpenAI Configuration (if LLM_PROVIDER=openai)
# Get key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o
OPENAI_BATCH_SIZE=50
OPENAI_RATE_LIMIT_RPM=3000

# OpenRouter Configuration (if LLM_PROVIDER=openrouter)
# Get key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-your-openrouter-key-here
OPENROUTER_API_BASE=https://openrouter.ai/api/v1
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# ----- Redis (for caching) -----
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# ----- Processing Configuration -----
# Number of chunks to process in parallel when learning from books
BOOK_PARALLEL_CHUNKS=10

# ----- Debug Options -----
# Set to 1 to enable debug output
AGENT_TRACE=0
SSE_DEBUG=0

# ----- CORS (for Docker) -----
BACKEND_CORS_ORIGINS=http://localhost:3000,http://frontend:3000

# ============================================
# SECURITY NOTES:
# ============================================
# 
# 1. NEVER commit your .env file to git
# 2. Rotate your API keys if they were ever exposed
# 3. Use different keys for development and production
# 4. Keep your Supabase SERVICE_ROLE_KEY secret
#    (it bypasses Row Level Security)
#
# ============================================
